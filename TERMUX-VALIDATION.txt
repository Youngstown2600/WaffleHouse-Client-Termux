WaffleHouse-Client-Termux Build 0.6 — validation
=========================================

Termux-specific parity gate: 43/43 PASS.
Shell syntax validation: PASS.

Latest SIP fixes:
- Saved SIP passwords survive 408/network/transport registration failures.
- Only explicit authentication failures may clear an unsaved session secret.
- Persisted SIP passwords are never silently erased by backend error recovery.
- PJ_EINVALIDOP during unregister of an already timed-out/offline account is treated as idempotent success.
- Termux TMPDIR runtime path handling retained.
- Android JNI PJSIP audio backend remains disabled; PortAudio path retained.

Previously validated Termux architecture remains intact:
- Dedicated Termux CLI entry point.
- Qt Core/Network only; no Qt GUI/Widgets/XKB desktop frontend.
- Termux pkg dependency installation; no CPAN/Perl command path.
- PJSIP 2.17 managed build with WaffleHouse account/call/ioqueue limits.
- AIM/OSCAR, IRC slash commands, Telnet/BBS/ANSI, SIP, Secure Rooms,
  encrypted/direct file transfer, media/streams, themes, Android storage,
  notification audio and URL handoff retained.

Native Android/Termux runtime remains the authoritative end-to-end test environment.
